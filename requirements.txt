# BlindAid - Preferred installation path
# =======================================
# The project now ships with a pyproject.toml. Installing the editable package with the
# "full" extra pulls every runtime dependency required for captions, depth, OCR, and TTS.

-e .[full]

# Development utilities (optional)
# -e .[dev]
