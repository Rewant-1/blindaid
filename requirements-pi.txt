ultralytics>=8.0.0
dlib>=19.22.0
gTTS>=2.3.0
# BlindAid - Raspberry Pi 5 Optimized Requirements
# ===============================================
# Installing via pyproject keeps everything consistent. On Raspberry Pi we pin the
# headless OpenCV build and skip the heavy caption/depth extras unless explicitly requested.

-e .

# Optional advanced models (enable only on Pi 5 8GB)
# torch>=2.0.0
# torchvision>=0.15.0
# transformers>=4.30.0
